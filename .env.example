VITE_LEARNER_DATA_SOURCE=api
VITE_API_BASE_URL=
VITE_BACKEND_URL=http://127.0.0.1:8080
VITE_GAZE_DEBUG_PANEL=false
# 커서(마우스)→시선 폴백 스위치 (opt-in, 기본값 꺼짐).
# 실 배포에서는 false — 마우스 좌표가 실제 시선 데이터로 저장되는 것을 방지한다.
# 아이트래커 없이 마우스로 데모하려면 true 로 설정한다.
VITE_CURSOR_GAZE_FALLBACK=true
